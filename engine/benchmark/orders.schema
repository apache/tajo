create table orders (
	o_orderkey int, /* primary key */
	o_custkey int,
	o_orderstatus string,
	o_totalprice float,
	o_orderdate date,
	o_orderpriority string,
	o_clerk string,
	o_shippriority int,
	o_comment string
) 
