create table orders (
	o_orderkey bigint, /* primary key */
	o_custkey bigint,
	o_orderstatus text,
	o_totalprice double,
	o_orderdate date,
	o_orderpriority text,
	o_clerk text,
	o_shippriority int,
	o_comment text
) 
